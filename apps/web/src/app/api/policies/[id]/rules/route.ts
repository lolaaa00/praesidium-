import { NextRequest, NextResponse } from 'next/server';

// List rules / Create rule



export async function GET(_request: NextRequest) {
  // TODO: Implement — List rules / Create rule
  return NextResponse.json({ message: 'GET policies/[id]/rules — not yet implemented' }, { status: 501 });
}

export async function POST(_request: NextRequest) {
  // TODO: Implement — List rules / Create rule
  return NextResponse.json({ message: 'POST policies/[id]/rules — not yet implemented' }, { status: 501 });
}
