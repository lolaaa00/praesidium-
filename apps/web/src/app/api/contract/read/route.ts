import { NextRequest, NextResponse } from 'next/server';

// Proxy read to GenLayer contract



export async function GET(_request: NextRequest) {
  // TODO: Implement — Proxy read to GenLayer contract
  return NextResponse.json({ message: 'GET contract/read — not yet implemented' }, { status: 501 });
}
