import { NextRequest, NextResponse } from 'next/server';

// List validation history



export async function GET(request: NextRequest) {
  // TODO: Implement — List validation history
  return NextResponse.json({ message: 'GET validations — not yet implemented' }, { status: 501 });
}
